# Copy this file to ~\env.profile.ps1 and fill in as needed.
# It is dot-sourced by Profile.ps1 and is gitignored — safe for
# machine-specific/local settings. Mirrors ../sample.env.zshrc.

$env:ENABLE_REACT_NATIVE = 'false'
$env:ENABLE_ASDF = 'false'
